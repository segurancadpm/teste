// ─── Adicionar ao state e ao defaultData ─────────────────────────────────────
// Em defaultData(), adicionar precos: {}
// Em ensureDataShape(), adicionar if (!state.data.precos) state.data.precos = {};

// ─── Remover orçamento da home ──────────────────────────────────────────────
// Em renderHome(), eliminar a chamada a renderBudgetCard()

// ─── Adicionar novo separador no bottomNav ──────────────────────────────────
// Em bottomNav(), adicionar item "budget" apenas se isSuper()

// ─── Criar página de orçamento ──────────────────────────────────────────────
// Função renderBudget() com tabela de preços, quantidades entregues e custos

// ─── Melhorias na auditoria ──────────────────────────────────────────────────
// Adicionar filtros e estatísticas adicionais
// Adicionar lista de trabalhadores problemáticos